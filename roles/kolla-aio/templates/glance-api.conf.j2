[DEFAULT]
show_image_direct_url = True

[glance_store]
stores = rbd
default_store = rbd
rbd_store_pool = {{ openstack_glance_pool.name }}
rbd_store_user = glance
rbd_store_ceph_conf = /etc/ceph/ceph.conf
rbd_store_chunk_size = 8

[image_format]
container_formats = bare
disk_formats = raw

[paste_deploy]
flavor = keystone
